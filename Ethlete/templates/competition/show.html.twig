{% extends 'back.html.twig' %}

{% block title %}Competition{% endblock %}

{% block body %}
    <h1>Competition</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdCompetition</th>
                <td>{{ competition.idCompetition }}</td>
            </tr>
            <tr>
                <th>NbEquipe</th>
                <td>{{ competition.nbEquipe }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ competition.date }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ competition.adresse }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ competition.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_competition_index') }}">back to list</a>

    <a href="{{ path('app_competition_edit', {'idCompetition': competition.idCompetition}) }}">edit</a>

    {{ include('competition/_delete_form.html.twig') }}
{% endblock %}
